\\Binary Search 
int low=0,high=n-1,mid;
        while(low<=high)
      {
        mid=(low+high)/2;
        
        if(arr[mid]==k)
        return mid;
        
        if(arr[mid]<k)
        low=mid+1;
        
        else
        high=mid-1;
      } 
        
       return -1; 